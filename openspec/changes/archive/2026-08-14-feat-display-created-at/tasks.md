## 1. Update list output in CLI

- [x] 1.1 Add `Created At` header to the list table in `snekdo/__main__.py`
- [x] 1.2 Add `created_at` value to each todo row in the list table
- [x] 1.3 Adjust the separator line length to accommodate the new column

## 2. Add tests

- [x] 2.1 Add test verifying the `Created At` header is present in list output
- [x] 2.2 Add test verifying the `created_at` value is displayed for each todo
- [x] 2.3 Update existing list tests to account for the new column

## 3. Verify

- [x] 3.1 Run `pytest` to ensure all tests pass
- [x] 3.2 Run `snekdo list` with sample data to verify the output visually
