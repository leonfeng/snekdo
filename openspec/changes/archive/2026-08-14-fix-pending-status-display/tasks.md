## 1. Update list display

- [x] 1.1 Change the status string for pending todos from a blank space to "pending" in `handle_list` in `snekdo/__main__.py`
- [x] 1.2 Change the status string for pending todos from a blank space to "pending" in `handle_show` in `snekdo/__main__.py`

## 2. Update tests

- [x] 2.1 Update `tests/test_cli.py` to expect "pending" instead of a blank space for pending status in list output
- [x] 2.2 Update `tests/test_cli.py` to expect "pending" instead of a blank space for pending status in show output

## 3. Verify

- [x] 3.1 Run `pytest` to confirm all tests pass
